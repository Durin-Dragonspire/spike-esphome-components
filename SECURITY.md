# Security

Do not report SPIKE Home Alarm credentials, secrets, configuration exports, or
private-repository material in public issues. This repository accepts only the
standalone compiler component prepared by the allowlist export process.

Module password and address binding detects an unexpected module that cannot
authenticate. It is not cryptographic hardware attestation and cannot defeat
an attacker who extracts valid credentials from trusted hardware.

